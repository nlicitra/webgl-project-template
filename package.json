{
  "name": "webgl-project-template",
  "version": "1.0.0",
  "description": "A project template for basic WebGL applications.",
  "main": "index.html",
  "scripts": {
    "start": "live-server",
    "build-example": "cp -r ./js/ ./example/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
